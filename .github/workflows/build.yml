name: Build and Release

on:
  push:
    branches: [ master ]

permissions:
  contents: write

jobs:
  build:
    runs-on: self-hosted
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.x'

    - name: Install dependencies
      shell: powershell
      run: |
        & "${{ env.pythonLocation }}\python.exe" -m pip cache purge
        & "${{ env.pythonLocation }}\python.exe" -m pip install --upgrade pip --force-reinstall
        & "${{ env.pythonLocation }}\python.exe" -m pip install -r requirements.txt --force-reinstall
        & "${{ env.pythonLocation }}\python.exe" -m pip install pyinstaller --force-reinstall

    - name: List files in the repository
      run: |
        ls -R
      shell: powershell

    - name: Build with PyInstaller
      shell: powershell
      run: pyinstaller --onefile --noconsole --name SOCD_Cleaner --icon="uttanka.ico" --add-data "uttanka.png;." --manifest manifest.xml main.py

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.run_number }}
        release_name: Release ${{ github.run_number }}
        draft: false
        prerelease: false

    - name: Upload Release Asset
      id: upload-release-asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./dist/SOCD_Cleaner.exe
        asset_name: SOCD_Cleaner.exe
        asset_content_type: application/vnd.microsoft.portable-executable
